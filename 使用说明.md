# 辅导员工作台 · 使用说明

> 本文件保留为仓库根目录的快速入口；完整的 v4.0 使用手册、平台构建状态和品牌素材说明请分别查看 [`docs/辅导员工作台使用手册.md`](./docs/辅导员工作台使用手册.md)、[`docs/平台构建矩阵.md`](./docs/平台构建矩阵.md) 与 [`docs/品牌与素材说明.md`](./docs/品牌与素材说明.md)。

一份给辅导员老师看的使用手册。不用懂代码，跟着做就行。

**当前版本：v4.0.0**。工作台同时提供 Windows/macOS 桌面版和单 HTML 网页版。

> 这是历史中文入口。最新的跨平台启动和文档索引请先看[文档中心](./docs/README.md)与[开始使用](./docs/getting-started.md)；本页保留详细业务步骤以兼容旧链接。

---

## 一、怎么打开

普通用户有三种选择：单 HTML 网页版无需安装，Windows/macOS 桌面版适合长期使用本地数据库和附件保险库。

### 单 HTML 网页版

1. 把 `index.html` 文件复制到你的电脑（桌面或某个文件夹都行）。
2. 用 **Chrome / Edge / 360 极速浏览器** 双击打开即可。
3. 第一次打开会看到一些示例数据（示例老师、张明、李思颖等），这是为了让你先看看效果，不是真实数据。

> 建议：用 Chrome 或 Edge 打开体验最好；手机也能用，但录数据还是在电脑上更顺手。

### Windows 桌面版

1. 下载发布包中的安装程序，或直接运行便携版。
2. 桌面版会把工作数据、照片和附件保存在本机，不需要浏览器窗口。
3. 开发者在仓库中运行 `pnpm install --frozen-lockfile` 和 `pnpm run desktop:dev`。

开发者也可以运行 `pnpm run web:dev`，再打开 `http://127.0.0.1:4173` 预览网页；构建网页发布包用 `pnpm run build:release`，构建桌面安装包用 `pnpm run desktop:build`。

### macOS 桌面版

1. 下载 `counselor-desk-4.0.0-mac-universal.dmg` 或同名 ZIP。
2. 打开 DMG 后将“辅导员工作台”拖入“应用程序”；Intel 和 Apple Silicon 使用同一个 universal 包。
3. 当前公开构建未配置 Apple Developer 签名与公证，首次打开若被 Gatekeeper 拦截，请按[桌面版安装与数据路径](./docs/v4-desktop-installation.md)中的步骤确认来源并允许启动。

---

## 二、第一次打开，先做两件事

打开后，首页顶部会有一条蓝色提示「当前展示的是示例数据」，照着做：

1. **清空示例数据**：点提示里的「清空示例数据」按钮（会让你确认一次）。清空后，页面就空了，可以录入你自己的学生。
2. **填上你的名字和学院**：左侧点「平台联动」，在「数据存储与备份」里填好「辅导员姓名」「学院 / 院系」，点空白处自动保存。

> 以后想看示例效果，重新双击文件会再次出现（清缓存或换电脑后）。

---

## 三、每天都在用的：首页「今天要处理」

打开工作台，首页最上面就是 **今天要处理**，它会自动把该做的事列出来：

- **红色** = 已经逾期的，优先处理；
- **今天该做** 和 **快到期** 的也会排在前面；
- 每条右边有「完成 / 处理」按钮，点一下就搞定；
- 昨天没做完的，会自动顺延到今天，不会偷偷消失。

下面还有几张卡片：在办任务数、本月谈话次数（含「较上月」对比）、近半年工作趋势图；另有「学生关注结构」环形图（心理危机未解除 / 重点关注 / 其余在读，中心显示需关注人数）和「数据洞察」卡（任务逾期率、本月谈话覆盖率、重点关注占比，超阈值自动变色提示），一眼掌握全貌。

---

## 四、怎么录入数据

左侧菜单按工作场景分好类了，常用的三个入口也在首页有快捷按钮：

- **工作任务**：点「新建工作任务」，填内容、截止日、优先级（P0 最重要），保存即可。完成的任务勾掉就行。
- **谈心谈话**：点「记一次谈心谈话」，选学生、填日期、谈话内容 / 问题研判 / 处理措施。需要跟进的打勾，到跟进日会自动出现在「今天要处理」。
- **学生台账**：点「新增学生」，录入姓名、学号、班级、关注等级、危机标记等。重点关注的学生会被自动汇总。

其他模块（校外住宿、假期去向、评优榜样、请销假、考勤、学业预警与帮扶、奖助勤补、心理摸排、就业、政策智库、素材收藏、科创竞赛、模板库等）用法类似，都是「进模块 → 点新建 → 填表 → 保存」。

---

## 五、找东西：顶栏搜索

页面最上方的搜索框，输入学生姓名、任务标题或谈话关键词，会**跨所有模块**实时列出结果。点任意一条，直接跳到对应页面并帮你筛好。比如搜「张明」，一点就到学生台账里张明的那条。

---

## 六、数据怎么保存、怎么带走（重要）

你的所有数据都**只存在你这台电脑的浏览器里**，关掉网页不会丢，**也不会上传到任何服务器**，隐私安全。

但有两件事请务必注意，否则换电脑或清缓存会丢数据：

### 1）换电脑 / 重装前，带走数据（二选一，建议都做）

进左侧「平台联动」→「数据存储与备份」：

- **生成便携工作台（数据已内置）** ⭐推荐：会生成一个带着你全部数据的 HTML 文件，名字像 `辅导员工作台-含数据-王老师_2026-08-04.html`。**双击它就能用**，最适合拷到 U 盘、发微信文件或存网盘，到别的电脑上直接用。
- **导出完整备份**：会得到一个 `.json` 文件。以后在任何电脑打开工作台，点「导入备份 / 交换包」选这个文件，数据就回来了（可选择合并或覆盖）。

### 2）多人共用一台电脑

数据是按浏览器存的。如果办公室电脑好几个人轮流用，每个人的数据会混在一起。建议：每人用自己的浏览器账号，或者每人生成一份「便携工作台」文件分开保管。

### 3）单个模块批量导入（比如从 Excel 搬数据）

每个模块的工具栏现在都有两个按钮：

- **模板**：下载该模块的 CSV 表头文件（带 BOM，Excel 直接打开不乱码）。先照着表头把数据填进 Excel，另存为 CSV；
- **导入 CSV**：选你填好的 CSV，数据就批量进来了。导出和导入用的是同一套表头，来回不丢字段。

> 用「导出 CSV」把现有数据导出来，改一改再「导入 CSV」回去，也是常见的批量修正方式。

另外，用上面的「导出完整备份」再「导入备份」时，现在会**一并把你填的辅导员姓名、学院、隐私锁密码带回来**，换电脑后不用重新设。

### 4）第一次用：学生大表 xls/xlsx 一键导入（v4.0）

打开「学生工作台 → 学生」模块，右上角点 **「导入大表」**：

1. 直接选你从学校信息化门户/学工系统导出的 `.xls` 或 `.xlsx` 文件（无需另存 CSV）；
2. 工作台会自动识别表头，**常见列名模糊匹配**——学号/姓名/性别/班级/专业/年级/学院/辅导员/政治面貌/手机/宿舍/身份证/邮箱/QQ/家长电话 等等，常见的各种叫法都能识别；
3. 缺值默认填空（比如某行没填"宿舍"就留空，不会报错也不会被填成 0）；
4. 同一学号二次导入会**按学号去重，新值补齐旧值，空值不覆盖**（不会重复建档）；
5. 导入完成后弹窗会告诉你：新增几条、更新几条、跳过了几条无学号/姓名的行。

> **离线提示**：xls/xlsx 解析需要从 CDN 动态加载 SheetJS（首次约 200KB），之后会缓存。如果在断网环境里用不了，第一次联网时打开过一次之后就能离线用；纯离线场景直接保存为 CSV 也能用。

---

## 七、各模块速览

| 分组 | 模块 | 用来做什么 |
|---|---|---|
| 工作台 | 今日概览 | 今天要处理、关键数字、半年趋势、学生关注结构环图、数据洞察卡 |
| 工作台 | 工作任务 | 待办清单、优先级、完成状态、批量操作 |
| 工作台 | 学生台账 | 学生档案、关注等级、危机标记 |
| 工作台 | 谈心谈话 | 谈话记录、跟进提醒 |
| 工作台 | 工作留痕 | 一键生成阶段 / 周 / 月工作小结 |
| 专项管理 | 校外住宿 / 假期去向 / 评优榜样 / 请销假 / 考勤台账 / 工作节点 | 各类专项工作记录 |
| 学业与资助 | 学业预警 / 学业帮扶 / 奖助勤补 | 学习预警与帮扶、奖助记录 |
| 重点关怀 | 重点学生档案 / 心理摸排 / 毕业生就业 | 重点关注与帮扶跟踪 |
| 资料平台 | 政策智库 / 素材收藏 / 科创竞赛 / 模板库 | 政策、素材、竞赛、常用模板 |
| 数据 | 平台联动 | 数据备份与恢复、便携版生成 |

---

## 八、常见问题

**Q：数据会丢吗？**
只要你不清浏览器缓存、不换电脑，数据一直在。为防万一，按第六节的办法定期「生成便携工作台」或「导出备份」。

**Q：换了一台新电脑，之前的记录还有吗？**
本地数据不跟随文件。请把旧电脑上的「便携工作台」文件拷过来双击打开，或把「完整备份 .json」导入新电脑的工作台。

**Q：不小心清空了全部数据，能恢复吗？**
如果之前导出过备份 / 便携版，重新导入即可。没导过就无法恢复，所以**养成定期备份的习惯**很重要。

**Q：示例数据怎么彻底去掉？**
首页蓝色提示里点「清空示例数据」，或进「平台联动 → 危险操作 → 清空示例数据」。只删示例，不影响你自己录的内容。

**Q：手机能用吗？**
能打开，也能录入，但屏幕小、打字慢，适合临时查看；日常录入建议在电脑上。

**Q：需要联网吗？**
不需要。完全离线，断网也能用。

**Q：可以切换深色模式吗？**
可以。顶栏右上角（备份 / 恢复 / 设置旁边）有一个太阳 / 月亮图标，点一下即可在浅色与深色之间切换；选择会存到本地，下次打开还是你上次的样子；第一次打开会跟随你系统的深色偏好。

---

> 提示：本工作台是纯本地工具，数据责任由使用者本人负责。涉及学生隐私的信息，请勿在公共电脑长期留存，用完记得导出便携版带走并清理。
