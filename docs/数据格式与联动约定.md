# 数据格式与联动约定

本文档定义辅导员工作台的本地数据格式、与重型「办公网站」平台的字段对照、交换包结构（v7），以及二次开发接口约定。目的是让本工作台未来能与平台**无缝联动**，且改造时可被 AI 可靠地理解和扩展。

---

## 1. 存储位置与命名空间

- 所有数据存于浏览器 `localStorage`；
- key 统一前缀：`cwb_v1_`；
- 数据以 JSON 字符串存储（写入时 `JSON.stringify`，读取时 `JSON.parse`）；
- 示例：

| key | 内容 | 版本 |
| --- | --- | --- |
| `cwb_v1_students` | 学生数组 | v1.0 |
| `cwb_v1_tasks` | 任务数组 | v1.0 |
| `cwb_v1_talks` | 谈话记录数组 | v1.0 |
| `cwb_v1_stay` | 校外住宿/走读数组 | v2.1 |
| `cwb_v1_leave` | 假期去向数组 | v2.1 |
| `cwb_v1_honor` | 评优榜样数组 | v2.1 |
| `cwb_v1_pleave` | 请销假数组 | v3.0.0 🆕 |
| `cwb_v1_attend` | 考勤记录数组 | v3.0.0 🆕 |
| `cwb_v1_node` | 工作节点数组 | v3.0.0 🆕 |
| `cwb_v1_warn` | 学业预警数组 | v3.0.0 🆕 |
| `cwb_v1_help` | 学业帮扶数组 | v3.0.0 🆕 |
| `cwb_v1_grant` | 奖助勤补数组 | v3.0.0 🆕 |
| `cwb_v1_focus` | 重点学生档案数组 | v3.0.0 🆕 |
| `cwb_v1_psych` | 心理摸排数组 | v3.0.0 🆕 |
| `cwb_v1_graduate` | 毕业生档案/就业数组 | v3.0.0 🆕 |
| `cwb_v1_policy` | 政策智库数组 | v3.0.0 🆕 |
| `cwb_v1_material` | 工作素材数组 | v3.0.0 🆕 |
| `cwb_v1_comp` | 科创竞赛数组 | v3.0.0 🆕 |
| `cwb_v1_tpl` | 模板库数组 | v3.0.0 🆕 |
| `cwb_v1_settings` | 设置（如辅导员姓名、是否初始化） | v1.0 |
| `cwb_v1_custom` | 自定义模块数据 | v1.0 |

> 单域名上限约 5MB，日常纯文本够用。不存图片等大文件。

### 1.1 首次欢迎设置

`cwb_v1_settings` 中的 `welcome_experience` 保存首次欢迎与每日提示偏好：`addressed_as`（称呼）、`greeting_enabled`（问候开关）、`quote_enabled`（金句开关）、`last_open_date`、`last_greeting_index` 和 `last_quote_id`。这些字段不改变业务实体主键，随 JSON / `.cwbk` 完整备份迁移；网页端与桌面端均不得把它们上传到远程服务。

---

## 2. 实体结构

### 2.1 学生 `student`

字段分**平台对齐字段**（与重型平台同名）、**校准后本地字段**（参照本院真实表格）、**其他本地扩展字段**。

| 字段 | 说明 | 类别 | 来源 |
| --- | --- | --- | --- |
| `student_number` | 学号 | platform | — |
| `full_name` | 姓名 | platform | — |
| `gender` | 性别 | platform | — |
| `class_name` | 班级 | platform | — |
| `major_name` | 专业 | platform | — |
| `grade` | 年级 | platform | — |
| `college_name` | 学院 | platform | — |
| `counselor_name` | 辅导员 | platform | — |
| `enrollment_status` | **学生当前状态**（在读/休学/复学/退学/毕业/结业） | platform | 成绩表校准 |
| `politics` | **政治面貌**（中共党员/预备党员/入党积极分子/共青团员/群众/其他） | local | 青春榜样校准 |
| `phone` | 手机号 | local | — |
| `dorm` | 宿舍 | local | — |
| `birthday` | 出生日期 | local-sensitive | 学生大表可选字段 |
| `hometown` | 籍贯 | local | 学生大表可选字段 |
| `home_addr` | 家庭地址 | local-sensitive | 学生大表可选字段 |
| `parent_name` | 家长姓名 | local-sensitive | 学生大表可选字段 |
| `parent_phone` | 家长电话 | local-sensitive | 学生大表可选字段 |
| `edu_years` | 学制 | local | 学生大表可选字段 |
| `nation` | 民族 | local | 学生大表可选字段 |
| `id_card` | 身份证号 | local-sensitive | 默认模板不提供 |
| `email` | 邮箱 | local | 学生大表可选字段 |
| `qq` | QQ | local | 学生大表可选字段 |
| `focus` | 重点关注类型（见下） | local | — |
| `focus_level` | 关注等级 L1/L2/L3/空 | local | — |
| `crisis_level` | **危机预警级别**（校级/院级/空） | local | 心理危机预警库校准 |
| `crisis_way` | **危机发现方式**（自我报告/普查发现/其他人发现/其他） | local | 心理危机预警库校准 |
| `crisis_relieved` | **是否已解除预警**（布尔） | local | 心理危机预警库校准 |
| `note` | 备注 | local | — |

规范化函数：`CWB.norm.student(s)`。示例数据会被打 `_demo:true` 标记。

重点关注类型 `focus` 取值（来自 `CWB.consts.FOCUS` 常量）：
`psych` 心理关注 / `study` 学业预警 / `econ` 经济困难 / `health` 身体状况 / `career` 就业困难 / `disc` 行为纪律 / `family` 家庭特殊。

### 2.2 任务 `task`

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一 ID |
| `title` | 标题 |
| `duty` | 职责归属（九大职责代号之一） |
| `priority` | 优先级 P0/P1/P2 |
| `source` | 来源：学校下发/学院安排/平台下发/自主计划/学生诉求 |
| `classes` | 涉及班级 |
| `due` | 截止日期 `YYYY-MM-DD` |
| `status` | 状态：todo/doing/done |
| `note` | 说明 |
| `platform_ref` | 关联平台任务 ID（可选，联动用） |
| `created_at` | 创建时间 ISO |
| `done_at` | 完成时间（status=done 时填写） |
| `_demo` | 示例标记 |

规范化函数：`CWB.norm.task(t)`。逾期判定：`daysFromToday(due) < 0 且 status != done`。

### 2.3 谈话 `talk`

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一 ID |
| `date` | 谈话日期 `YYYY-MM-DD` |
| `student_number` | 学号 |
| `student_name` | 学生姓名 |
| `way` | 方式：面谈/电话/线上/家访/集体 |
| `duty` | 所属职责 |
| `summary` | 谈话内容摘要 |
| `judge` | 研判结论 |
| `action` | 处理措施 |
| `need_follow` | 是否需回访（布尔） |
| `follow_date` | 计划回访日期 |
| `done_follow` | 回访是否完成 |
| `created_at` | 创建时间 |
| `_demo` | 示例标记 |

规范化函数：`CWB.norm.talk(t)`。

### 2.4 校外住宿/走读 `stay` 🆕

| 字段 | 说明 | 参照来源 |
| --- | --- | --- |
| `id` | 唯一 ID | — |
| `student_name` | 学生姓名 | — |
| `student_number` | 学号 | — |
| `grade_major` | 年级专业（如「24机器人02」） | 校外住宿表 |
| `stu_type` | 学生类型：本科生/研究生 | 校外住宿表 |
| `reason` | 申请理由 | 校外住宿表 |
| `start` | 走读开始日期 | 校外住宿表 |
| `end` | 走读结束日期 | 校外住宿表 |
| `emergency_name` | 紧急联系人姓名 | 校外住宿表 |
| `emergency_phone` | 紧急联系人电话 | 校外住宿表 |
| `approval` | 审批状态：审批通过/审批中/待审批/未通过 | 校外住宿表 |
| `note` | 备注 | — |
| `created_at` | 创建时间 | — |
| `_demo` | 示例标记 | — |

规范化函数：`CWB.norm.stay(s)`。

### 2.5 假期去向 `leave` 🆕

| 字段 | 说明 | 参照来源 |
| --- | --- | --- |
| `id` | 唯一 ID | — |
| `name` | 学生姓名 | — |
| `student_number` | 学号 | — |
| `class_major` | 专业班级 | 去向统计表 |
| `home_addr` | 家庭所在省市区 | 去向统计表 |
| `leave_date` | 离校时间 | 去向统计表 |
| `return_date` | 返校时间 | 去向统计表 |
| `dorm` | 宿舍号 | 去向统计表 |
| `phone` | 本人手机号 | 去向统计表 |
| `delayed` | 是否延迟离校：是/否 | 去向统计表 |
| `delay_reason` | 延迟离校原因 | 去向统计表 |
| `teacher` | 指导老师 | 去向统计表 |
| `consent` | 家长知情同意书：已签/未签 | 去向统计表 |
| `note` | 特殊情况备注 | 去向统计表 |
| `created_at` | 创建时间 | — |
| `_demo` | 示例标记 | — |

规范化函数：`CWB.norm.leave(l)`。

### 2.6 评优榜样 `honor` 🆕

| 字段 | 说明 | 参照来源 |
| --- | --- | --- |
| `id` | 唯一 ID | — |
| `name` | 学生姓名 | — |
| `student_number` | 学号 | — |
| `major` | 专业 | 青春榜样表 |
| `class_name` | 行政班 | 青春榜样表 |
| `apply_type` | 申请类型（先锋/德育/智育/体育/美育/劳育） | 青春榜样表 |
| `politics` | 政治面貌 | 青春榜样表 |
| `rank` | 综合成绩专业排名（如「8/42」） | 青春榜样表 |
| `moral` | 德育素质考核等级（优/良/及格） | 青春榜样表 |
| `scholarships` | 入学来获奖学金情况（多行文本） | 青春榜样表 |
| `social` | 社会工作情况（多行文本） | 青春榜样表 |
| `others` | 其他获奖情况（多行文本） | 青春榜样表 |
| `created_at` | 创建时间 | — |
| `_demo` | 示例标记 | — |

规范化函数：`CWB.norm.honor(h)`。

申请类型完整映射：

| 代码 | 全称 |
| --- | --- |
| 先锋 | 低空先锋 |
| 德育 | 德育之星 |
| 智育 | 智育之星 |
| 体育 | 体育之星 |
| 美育 | 美育之星 |
| 劳育 | 劳育之星 |

---

### 2.7 请销假 `pleave` 🆕

| 字段 | 说明 | 参照来源 |
| --- | --- | --- |
| `id` | 唯一 ID | — |
| `student_number` | 学号 | — |
| `name` | 学生姓名 | — |
| `class_name` | 班级 | — |
| `start` / `end` | 请假起止日期 | 请销假台账 |
| `type` | 请假类型：事假/病假/公假/其他 | 请销假台账 |
| `reason` | 事由 | — |
| `approver` | 审批人 | — |
| `status` | 审批状态：待审批/已通过/已驳回 | — |
| `cancel` | 是否已销假（布尔） | — |
| `leave_at` | 请假提交日期 | — |
| `note` | 备注 | — |
| `created_at` | 创建时间 | — |
| `_demo` | 示例标记 | — |

规范化函数：`CWB.norm.pleave(p)`。待审批的请假会在首页提示；已通过且未销假且假期结束的会在首页提示「销假提醒」。

### 2.8 考勤 `attend` 🆕

| 字段 | 说明 | 参照来源 |
| --- | --- | --- |
| `id` | 唯一 ID | — |
| `student_number` | 学号 | — |
| `name` | 学生姓名 | — |
| `class_name` | 班级 | — |
| `date` | 考勤日期 | 考勤表 |
| `type` | 类型：旷课/迟到/早退/请假/病假 | 考勤表 |
| `duration` | 节次/时长 | — |
| `teacher` | 任课教师 | — |
| `note` | 备注 | — |
| `created_at` | 创建时间 | — |
| `_demo` | 示例标记 | — |

规范化函数：`CWB.norm.attend(a)`。登记时可关联学生台账自动带出学号/班级。

### 2.9 工作节点 `node` 🆕

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一 ID |
| `title` | 节点标题 |
| `category` | 所属职责（九项职责代号，决定首页 `tag-duty` 显示） |
| `due` | 截止日期 `YYYY-MM-DD` |
| `remind_days` | 提前提醒天数（默认 3） |
| `status` | 状态：todo/done |
| `repeat` | 重复：none/week/month/term/year |
| `summary` | 工作小结 |
| `attachment` | 附件说明（文本） |
| `done_at` | 完成日期 |
| `created_at` | 创建时间 |
| `_demo` | 示例标记 |

规范化函数：`CWB.norm.node(n)`。进入「提前提醒天数」或已逾期的节点会在首页提示。

### 2.10 学业预警 `warn` 🆕

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一 ID |
| `student_number` | 学号 |
| `name` | 学生姓名 |
| `class_name` | 班级 |
| `type` | 类型：挂科过多/学分不足/旷课过多/学业困难/其他 |
| `level` | 级别：红/橙/黄 |
| `course` | 相关课程 |
| `measure` | 已采取措施 |
| `follow` | 后续跟进计划 |
| `resolved` | 是否已解除（布尔） |
| `resolved_at` | 解除日期 |
| `note` | 备注 |
| `created_at` | 创建时间 |
| `_demo` | 示例标记 |

规范化函数：`CWB.norm.warn(w)`。**红色未解除**的预警在首页**红色置顶**，可一键「标记解除」或「建帮扶记录」。

### 2.11 学业帮扶 `help` 🆕

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一 ID |
| `student_number` | 学号 |
| `name` | 学生姓名 |
| `class_name` | 班级 |
| `type` | 类型：学业帮扶/经济帮扶/心理帮扶/就业帮扶 |
| `helper` | 帮扶人 |
| `cycle` | 帮扶周期 |
| `measure` | 帮扶措施 |
| `effect` | 帮扶效果 |
| `status` | 状态：跟踪中/已结束 |
| `note` | 备注 |
| `created_at` | 创建时间 |
| `_demo` | 示例标记 |

规范化函数：`CWB.norm.help(h)`。可由学业预警一键转入。

### 2.12 奖助勤补 `grant` 🆕

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一 ID |
| `student_number` | 学号 |
| `name` | 学生姓名 |
| `class_name` | 班级 |
| `kind` | 类型：奖学金/助学金/勤工助学/困难补助 |
| `amount` | 金额（文本，便于填写「2000/月」等） |
| `batch` | 批次/年度 |
| `status` | 发放状态：待发放/已发放 |
| `note` | 备注 |
| `created_at` | 创建时间 |
| `_demo` | 示例标记 |

规范化函数：`CWB.norm.grant(g)`。KPI 展示总额与待发放数。

### 2.13 重点学生档案 `focus` 🆕（隐私保护锁）

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一 ID |
| `student_number` | 学号 |
| `name` | 学生姓名 |
| `class_name` | 班级 |
| `category` | 类别：心理/经济/学业/家庭/行为/其他 |
| `level` | 等级：一级/二级/三级 |
| `reason` | 关注原因 |
| `measures` | 干预措施 |
| `follow_up` | 跟进计划 |
| `owner` | 负责老师 |
| `status` | 状态：跟踪中/已结案/已转介/已脱敏 |
| `updated_at` | 最近更新日期 |
| `created_at` | 创建时间 |
| `_demo` | 示例标记 |

规范化函数：`CWB.norm.focus(f)`。首次进入需设 **4 位密码**，查看需解锁；**一级·跟踪中**在首页置顶。**数据存本地、不外传**，密码仅为防误触，非加密。

### 2.14 心理摸排 `psych` 🆕

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一 ID |
| `student_number` | 学号 |
| `name` | 学生姓名 |
| `class_name` | 班级 |
| `screen_date` | 摸排日期 |
| `scale` | 量表：PHQ-9（抑郁）/GAD-7（焦虑）/SCL-90（综合）/SAS（焦虑自评）/自编问卷/其他 |
| `score` | 得分（数字） |
| `level` | 等级：正常/轻度/中度/重度 |
| `concern` | 关注点 |
| `suggestion` | 建议 |
| `handler` | 处理人 |
| `status` | 状态：跟踪中/已结案/无需跟进 |
| `created_at` | 创建时间 |
| `_demo` | 示例标记 |

规范化函数：`CWB.norm.psych(p)`。**重度·未结案**在首页置顶并提示处理人。

### 2.15 毕业生档案与就业 `graduate` 🆕

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一 ID |
| `student_number` | 学号 |
| `name` | 学生姓名 |
| `class_name` | 班级 |
| `major_name` | 专业 |
| `graduation_year` | 毕业年份 |
| `employment_status` | 就业状态：已就业/升学（国内）/出国出境/公务员（录用）/应征入伍/创业/待就业/暂不就业 |
| `employer` | 单位 |
| `job_type` | 岗位 |
| `location` | 地点 |
| `salary_range` | 薪酬区间：<6k/6k-10k/10k-15k/15k-20k/>20k/— |
| `signed` | 是否签约：是/否 |
| `updated_at` | 最近更新日期 |
| `created_at` | 创建时间 |
| `_demo` | 示例标记 |

规范化函数：`CWB.norm.graduate(g)`。KPI 展示就业率、升学出国、公务员录用。

### 2.16 政策智库 `policy` 🆕

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一 ID |
| `title` | 标题 |
| `category` | 分类：思想政治/学籍管理/资助育人/就业指导/心理健康/安全稳定/其他 |
| `issuer` | 发布单位 |
| `pub_date` | 发布日期 |
| `doc_no` | 文号 |
| `keywords` | 关键词（空格分隔） |
| `summary` | 摘要 |
| `url` | 链接 |
| `starred` | 是否收藏（布尔） |
| `created_at` | 创建时间 |
| `_demo` | 示例标记 |

规范化函数：`CWB.norm.policy(p)`。顶部搜索框支持标题/单位/关键词全文检索；可标记收藏。

### 2.17 工作素材 `material` 🆕

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一 ID |
| `title` | 标题 |
| `category` | 分类：讲话稿/班会PPT/活动方案/典型案例/图文素材/制度文件 |
| `source` | 来源 |
| `tags` | 标签（空格分隔） |
| `content` | 正文/内容 |
| `note` | 备注 |
| `created_at` | 创建时间 |
| `_demo` | 示例标记 |

规范化函数：`CWB.norm.material(m)`。便于班会、培训时快速取用。

### 2.18 科创竞赛 `comp` 🆕

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一 ID |
| `name` | 竞赛名称 |
| `category` | 类别：数学建模/电子设计/互联网+/挑战杯/机器人大赛/智能车/创新创业 |
| `organizer` | 主办单位 |
| `deadline` | 报名截止日期 |
| `members` | 参赛学生 |
| `award` | 获奖情况：未参赛/已报名/校级奖/省级奖/国家级奖 |
| `note` | 备注 |
| `created_at` | 创建时间 |
| `_demo` | 示例标记 |

规范化函数：`CWB.norm.comp(c)`。**临近截止（14 天内）或逾期**的赛事会在首页提醒。

### 2.19 模板库 `tpl` 🆕

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一 ID |
| `title` | 模板标题 |
| `type` | 类型：通知/流程/表单 |
| `scene` | 适用场景 |
| `content` | 模板正文 |
| `note` | 备注 |
| `created_at` | 创建时间 |
| `_demo` | 示例标记 |

规范化函数：`CWB.norm.tpl(t)`。支持一键复制复用，减少重复起草。

---

## 3. 与平台字段对照

重型平台（办公网站）核心表与工作台对齐关系：

| 平台表 | 平台字段 | 工作台字段 | 对齐 |
| --- | --- | --- | --- |
| students | `student_number` | `student_number` | ✅ 同名 |
| students | `full_name` | `full_name` | ✅ 同名 |
| students | `gender` | `gender` | ✅ 同名 |
| students | `enrollment_status` | `enrollment_status` | ✅ 同名（v2.1 校准为「学生当前状态」） |
| students | `graduation_cohort` | （预留） | 字段预留 |
| students | `student_type` | （预留） | 字段预留 |
| students | `active` | （预留） | 字段预留 |
| student_profiles | `class_name` | `class_name` | ✅ 同名 |
| student_profiles | `major_name` | `major_name` | ✅ 同名 |
| student_profiles | `grade` | `grade` | ✅ 同名 |
| student_profiles | `college_name` | `college_name` | ✅ 同名 |
| student_profiles | `counselor_name` | `counselor_name` | ✅ 同名 |
| student_profiles | `enrollment_month` | （预留） | 字段预留 |

> 前 8 个平台字段与工作台同名，导入导出零转换成本。`graduation_cohort` / `student_type` / `active` / `enrollment_month` 在平台侧存在，工作台已预留扩展位，后续如需对接可直接复用 `schema.student` 扩展。

---

## 4. 交换包 v7 结构（JSON）

v7 在原有业务集合基础上增加 `schema_version`、稳定时间戳、附件清单和可迁移附件内容。`attachments_manifest` 只保存附件元数据与内容哈希；完整备份或便携 HTML 才会按大小限制内嵌附件内容。

`CWB.bridge.buildPackage(scope)` 产出（`PACKAGE_VERSION = 6`）：

```json
{
  "package": "counselor-desk",
  "package_version": 7,
  "app_version": "3.9.0",
  "exported_at": "2026-08-04T00:00:00Z",
  "counselor": {
    "name": "",
    "college_name": "",
    "org_code": ""
  },
  "scope": {},
  "students":  [ { ... }, ... ],
  "tasks":     [ { ... }, ... ],
  "talks":     [ { ... }, ... ],
  "stay":      [ { ... }, ... ],
  "leave":     [ { ... }, ... ],
  "honor":     [ { ... }, ... ],
  "pleave":    [ { ... }, ... ],
  "attend":    [ { ... }, ... ],
  "node":      [ { ... }, ... ],
  "warn":      [ { ... }, ... ],
  "help":      [ { ... }, ... ],
  "grant":     [ { ... }, ... ],
  "focus":     [ { ... }, ... ],
  "psych":     [ { ... }, ... ],
  "graduate":  [ { ... }, ... ],
  "policy":    [ { ... }, ... ],
  "material":  [ { ... }, ... ],
  "comp":      [ { ... }, ... ],
  "tpl":       [ { ... }, ... ],
  "learning_materials": [ { ... }, ... ],
  "learning_notes":     [ { ... }, ... ],
  "learning_sessions":  [ { ... }, ... ],
  "custom":    {},
  "stats": {
    "student_total": 0,
    "focus_total": 0,
    "crisis_total": 0,
    "task_total": 0,
    "task_done": 0,
    "talk_total": 0,
    "stay_total": 0,
    "leave_total": 0,
    "honor_total": 0,
    "pleave_total": 0,
    "pleave_pending": 0,
    "attend_total": 0,
    "node_total": 0,
    "node_overdue": 0,
    "warn_total": 0,
    "warn_active": 0,
    "warn_red": 0,
    "help_total": 0,
    "help_tracking": 0,
    "grant_total": 0,
    "grant_pending": 0,
    "focus_total": 0,
    "focus_lvl1": 0,
    "psych_total": 0,
    "psych_severe": 0,
    "graduate_total": 0,
    "graduate_employed": 0,
    "policy_total": 0,
    "policy_starred": 0,
    "material_total": 0,
    "comp_total": 0,
    "comp_registered": 0,
    "tpl_total": 0,
    "by_duty": [
      { "duty":"ideology", "duty_name":"...", "tasks":0, "tasks_done":0, "talks":0 },
      ...
    ]
  }
}
```

### scope 参数

不传或传 `{}` → 导出全部数据。
可按范围过滤：
```js
bridge.buildPackage({ from:'2026-01-01', to:'2026-06-30' })  // 按日期范围
bridge.buildPackage({ students:false })                   // 排除学生数据
```

### 导入模式

`CWB.bridge.applyPackage(pkg, mode)`
- `'merge'`（推荐）：按 `id`（学生按 `student_number`）合并，已存在则覆盖字段，不存在则追加；
- `'replace'`：直接用包内数据覆盖本地（先规范化）。

> 导入会自动识别两种格式：本工作台导出的包（`pkg.package === 'counselor-desk'`）和通用格式（取 `pkg.data` 或 `pkg` 本身）。导入前会验证至少有一个非空数组。

平台侧只要能导出相同结构的 JSON，即可在本工作台「导入交换包」；反之亦然。

### 版本兼容

| PACKAGE_VERSION | 支持 |
| --- | --- |
| 1 | students / tasks / talks / custom |
| 2 | + stay / leave / honor + crisis_total / 各专项 total |
| 3 | + pleave / attend / node / warn / help / grant（第一期 + 第二期） |
| 4 | + focus / psych / graduate（第三期） |
| **5** | + policy / material / comp / tpl + 各专项 stats 全量（第四期） |
| **6（当前）** | + learning_materials / learning_notes / learning_sessions（本地学习助手） |

导入时会自动适配 v1–v7 全部版本（`applyPackage` 按 `id` 合并、按 `student_number` 合并学生）。缺少学习助手数组或附件清单的旧包仍可正常导入。

---

## 4.1 v3.9 学习助手数据

学习助手保持本地优先，三组数组随交换包 v7 一起迁移：

| 数据集 | 关键字段 | 用途 |
| --- | --- | --- |
| `learning_materials` | `id`, `title`, `category`, `content`, `summary`, `status`, `progress` | 学习资料、摘要与进度 |
| `learning_notes` | `id`, `material_id`, `content`, `tags`, `created_at` | 资料笔记和标签 |
| `learning_sessions` | `id`, `material_id`, `date`, `minutes`, `note` | 学习时长与复盘 |

摘要由本地规则生成，不默认调用外部 AI；后续接入 AI 时应保持本地数据可独立使用，并在用户确认后发送内容。

---

## 5. CSV 约定

- 导出的 CSV 带 **UTF-8 BOM**（`\uFEFF` 前缀），Excel 打开中文不乱码；
- 所有列表类视图均支持「导出 CSV」按钮；
- CSV 表头格式：`中文名(英文字段名)`，便于人工阅读和程序解析；
- 解析用 `CWB.utils.parseCSV`，兼容基础引号转义；
- 日期格式统一 `YYYY-MM-DD`；
- 学生导入支持中文表头自动映射（见 `CSV_MAP` 常量）。

### 各模块 CSV 表头

**学生台账**：`学号(student_number) / 姓名(full_name) / 性别(gender) / 班级(class_name) / 专业(major_name) / 年级(grade) / 学院(college_name) / 辅导员(counselor_name) / 学生当前状态(enrollment_status) / 政治面貌(politics) / 联系电话(phone) / 宿舍(dorm) / 关注等级(focus_level) / 关注类型(focus) / 备注(note)`

**工作任务**：`任务名称 / 职责分类 / 来源 / 涉及班级 / 截止日期 / 优先级 / 状态 / 备注 / 平台任务号`

**谈心谈话**：`日期 / 学号 / 姓名 / 方式 / 主题分类 / 谈话内容 / 问题研判 / 处理措施 / 需跟进 / 跟进日期 / 跟进完成`

**校外住宿**：`姓名 / 学号 / 年级专业 / 学生类型 / 申请理由 / 走读开始 / 走读结束 / 紧急联系人 / 紧急联系人电话 / 审批情况 / 备注`

**假期去向**：`姓名 / 学号 / 专业班级 / 家庭所在省市区 / 离校时间 / 返校时间 / 宿舍 / 本人手机号 / 是否延迟离校 / 留校缘由 / 指导老师 / 家长知情同意书 / 特殊情况备注`

**评优榜样**：`姓名 / 学号 / 申请类型 / 政治面貌 / 专业 / 班级 / 综合成绩专业排名 / 德育素质考核等级 / 入学来获奖学金情况 / 社会工作情况 / 其他获奖情况`

**请销假**：`姓名 / 学号 / 班级 / 开始 / 结束 / 类型 / 事由 / 审批人 / 审批状态 / 是否销假 / 备注`

**考勤**：`姓名 / 学号 / 班级 / 日期 / 类型 / 节次 / 任课教师 / 备注`

**工作节点**：`标题 / 职责 / 截止 / 提前提醒天数 / 状态 / 重复 / 小结`

**学业预警**：`姓名 / 学号 / 班级 / 类型 / 级别 / 课程 / 措施 / 跟进 / 已解除 / 备注`

**学业帮扶**：`姓名 / 学号 / 班级 / 类型 / 帮扶人 / 周期 / 措施 / 效果 / 状态 / 备注`

**奖助勤补**：`姓名 / 学号 / 班级 / 类型 / 金额 / 批次 / 状态 / 备注`

**重点学生档案**：`姓名 / 学号 / 班级 / 类别 / 等级 / 原因 / 措施 / 跟进 / 负责人 / 状态`

**心理摸排**：`姓名 / 学号 / 班级 / 日期 / 量表 / 得分 / 等级 / 关注点 / 建议 / 处理人 / 状态`

**毕业生就业**：`姓名 / 学号 / 班级 / 专业 / 毕业年份 / 就业状态 / 单位 / 岗位 / 地点 / 薪酬区间 / 是否签约`

**政策智库**：`标题 / 分类 / 发布单位 / 发布日期 / 文号 / 关键词 / 摘要 / 链接 / 收藏`

**工作素材**：`标题 / 分类 / 来源 / 标签 / 内容 / 备注`

**科创竞赛**：`名称 / 类别 / 主办单位 / 报名截止 / 参赛学生 / 获奖情况 / 备注`

**模板库**：`标题 / 类型 / 适用场景 / 正文 / 备注`

---

## 6. API 直连预留（未来）

`CWB.bridge` 已声明以下方法，当前为占位，实现 `request` 后可启用：

- `request(path, init)` —— 统一的网络请求封装（需自行实现 fetch + 鉴权 + 跨域处理）；
- `pullTasks()` —— 从平台拉取任务写入本地；
- `pushSummary(scope)` —— 把本地工作小结推送到平台。

> 安全边界：本地数据默认不外传。任何直连都需在用户明确授权下进行，且不得把含真实学生信息的备份文件未经脱敏提交到公开仓库。

---

## 7. 二次开发接口约定

- 新数据 key 一律 `cwb_v1_` 前缀；
- 新增实体字段改 `CWB.schema.*` 并在对应 `norm*()` 中补默认值的兼容处理；
- 自定义模块用 `CWB.modules.register(def)`，导航自动插入；
- 渲染用户输入前必须 `CWB.utils.esc()`，防 XSS；
- 运行时不得引入外部 CDN、框架或图表库；发布包使用仓库内固定版本的 SheetJS 离线解析器；
- 不得 import 或依赖「办公网站」原项目代码（保持仓库隔离）；
- 新增业务模块建议参照本院真实台账文件校准字段命名（v2.1 的三个新模块即为范例）。
