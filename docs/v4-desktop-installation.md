# Windows / macOS 桌面版安装与数据路径

适用版本：`v4.0.0`。这份说明只面向普通使用者，开发命令请看 [贡献指南](../CONTRIBUTING.md)。

## 下载哪个文件

普通用户从 [GitHub Releases](https://github.com/7752777/counselor-desk/releases) 的“未签名体验版”附件中选择一个文件。源码仓库不会提交 `.msi`、`.dmg`、`.zip` 等安装产物，以免仓库膨胀或绕过发布校验；这些文件只通过 Release 附件分发：

| 文件 | 推荐对象 | 使用方式 |
| --- | --- | --- |
| `辅导员工作台-v4.0.0-Windows-安装版.msi` | 需要日常固定使用的 Windows 电脑 | 双击后自动安装，并创建桌面和开始菜单入口 |
| `辅导员工作台-v4.0.0-网页离线版.html` | 临时电脑、U 盘或不想安装的场景 | 双击即可离线使用，数据保存在当前浏览器 |
| `辅导员工作台-v4.0.0-macOS-安装版.dmg` | Intel 与 Apple Silicon Mac | 打开后将应用拖入“应用程序” |
| `辅导员工作台-v4.0.0-macOS-压缩包.zip` | 不方便使用 DMG 的 Mac | 解压后将应用拖入“应用程序” |

普通用户不需要安装 Node.js、pnpm、Electron，也不需要打开 PowerShell。安装版采用当前用户安装，不要求管理员权限；首次启动后会自动打开工作台。

## 第一次启动

1. 双击 `辅导员工作台-v4.0.0-Windows-安装版.msi`，等待安装完成。
2. 从桌面快捷方式或开始菜单打开“辅导员工作台”。
3. 首次进入先完成欢迎卡片：填写姓名 / 称呼并选择主题；然后选择“体验示例”熟悉页面，或选择“正式初始化”填写个人信息、导入学生名单并完成首次备份。
4. 确认“学生台账”和“备份与迁移”可以打开后，再导入真实学生数据。之后每天首次打开会显示暖心问候和教育金句，可在设置中关闭。

Windows MSI 安装版与 macOS 桌面包共用同一套业务界面。桌面版的数据默认只保存在本机，不需要账号，也不会自动上传到项目服务器。

## macOS 第一次启动

1. 打开 `辅导员工作台-v4.0.0-macOS-安装版.dmg`，把“辅导员工作台”拖到“应用程序”。
2. 从“应用程序”或 Launchpad 打开工作台；首次进入选择“体验示例”“正式初始化”或“从备份恢复”。
3. 如果 macOS 提示“无法验证开发者”，请确认安装包来自项目 Release，并在“系统设置 → 隐私与安全性”中允许本次打开。当前公开构建未配置 Apple Developer 签名和公证，不要把未知来源的副本加入信任。
4. Intel 和 Apple Silicon 使用同一个 universal 包，不需要另外判断芯片架构。

## 文件资料库与保险库路径

在“模板库 → 分类文件库”页面上传假期去向表、德育考核表、政策文件、讲话稿或安全提示。系统会按文件名自动分类、规范命名，并保留原文件；桌面版会把附件写入加密保险库。

不要根据仓库目录、下载目录或示例截图猜测数据位置。请直接在“分类文件库”页面查看“桌面版已启用加密附件保险库”后面的实际路径。该路径由 Electron 的用户数据目录决定，可能因 Windows 用户名、安装方式和测试参数不同而不同。

备份文件则由你在“平台联动 → 备份迁移”中选择保存目录。建议：

- 日常自动备份选择 OneDrive、移动硬盘或学校规定的受控文件夹；
- 备份文件使用 `.cwbk` 扩展名，口令由使用者单独保管；
- 更换电脑时先复制备份文件，再在新电脑选择“恢复 .cwbk 备份”；
- 不要把含有真实学生信息的备份上传到 Issue、网盘公开链接或代码仓库。

## 网页离线版注意事项

网页离线版适合临时使用，数据保存在当前浏览器的本地存储中。macOS 的 DMG/ZIP 只是分发方式，应用运行后仍会在当前用户的应用数据目录保存数据库和附件。不要只复制一个 HTML 或 `.app` 就认为数据已随文件移动；换电脑前请使用工作台内的加密备份或换机包。

## 无法打开时

- Windows 弹出“未知发布者”：当前公开构建没有组织代码签名证书，请只从项目 Release 下载，并核对附件 `Windows-SHA256校验和.txt`；macOS 对应 `macOS-SHA256校验和.txt`。
- macOS 弹出“无法验证开发者”：当前公开构建没有 Apple Developer 签名与公证，请只从项目 Release 下载并核对 `macOS-SHA256校验和.txt`，再按上面的“隐私与安全性”步骤一次性允许打开。
- 双击没有窗口：先检查任务管理器中是否已有“辅导员工作台”进程，再重新打开；仍失败时使用网页离线版确认业务数据无误，并附上 Windows 版本和错误截图。
- 数据看起来为空：确认打开的是同一个 Windows 用户和同一个备份恢复后的工作台，不要把仓库里的 `index.html` 与桌面版数据库混为一处。

## 开发者构建

在仓库根目录运行：

```powershell
pnpm install --frozen-lockfile
pnpm run desktop:build
```

构建 Windows 产物使用 `pnpm run desktop:build`；macOS 通用产物必须在 macOS runner 使用 `pnpm run desktop:build:mac`。两者都写入仓库相对路径 `output/desktop/`。该目录被 `.gitignore` 忽略，不应提交到公开库；发布时将对应安装包上传到 GitHub Release。
