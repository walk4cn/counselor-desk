# 安全策略 · Security Policy

## 支持的版本

下表列出了当前获得安全更新支持的版本：

| 版本 | 支持状态 |
| --- | --- |
| 3.9.x | ✅ 活跃支持 |
| 3.8.x | ✅ 活跃支持 |
| 3.7.x | ⚠️ 仅严重漏洞 |
| 3.6.x | ❌ 已停止支持 |
| < 3.6 | ❌ 已停止支持 |

## 报告漏洞

我们非常重视安全问题。如果你发现了潜在的安全漏洞，请**不要**在公开的 Issue 中提出。

### 报告方式

请通过以下私密渠道报告：

1. **GitHub Security Advisories**：[创建私密安全公告](../../security/advisories/new)

### 报告内容请包括

- 漏洞的简要描述
- 复现步骤 / PoC
- 影响范围（哪些版本受影响）
- 可能的修复建议（如果有）

### 响应时间

- **确认收到**：48 小时内
- **初步评估**：7 天内
- **修复发布**：视严重程度而定
  - 严重漏洞：48 小时内
  - 高危：7 天内
  - 中危：30 天内
  - 低危：下次例行更新

## 数据隐私说明

辅导员工作台是一个**纯本地**应用：

- ✅ 网页端业务数据优先存储在 IndexedDB（`localStorage` 仅作兼容回退）；桌面端存储在系统用户目录 SQLite/JSON 快照与受管理附件库
- ✅ 不上传任何服务器
- ✅ 无后端、无 API 调用（除可选的 SheetJS CDN 加载）
- ✅ 用户对数据拥有完全控制权

> ⚠️ **本地存储 ≠ 加密**。请勿在公用电脑或共享设备上记录真实学生信息。重点档案的 4 位密码锁只限制界面访问，不等同于对浏览器数据的密码学加密。

## 已知安全考虑

- **本地数据**：IndexedDB、SQLite/JSON 快照和附件文件都不是加密存储；界面锁只提供访问控制，不等同于数据库加密。在公用电脑使用后请导出完整备份并清空数据。
- **JSON 备份文件**：包含全量数据，传输时请使用可信渠道（加密邮件、加密 U 盘等）。
- **CSV 导入**：用户上传的 CSV 会通过工作台的解析器处理，请确保数据来源可信。
- **Excel 导入**：xls/xlsx 直传会按需从 SheetJS CDN 加载解析器；CSV、JSON 备份和已保存的本地数据不依赖后端。离线环境请使用 CSV。

## 安全更新政策

- 安全修复会尽快发布，通常作为补丁版本（如 3.8.1）
- 重大安全更新会提前公告（如确实需要）
- 我们承诺在修复发布前不会公开漏洞细节

---

再次感谢你帮助我们维护辅导员工作台的安全！🔒
